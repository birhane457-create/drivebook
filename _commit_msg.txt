feat: admin user management UI

New pages and API:
- app/admin/register/page.tsx: Full admin user management (replaces bootstrap form)
  - List all ADMIN/SUPER_ADMIN users with permissions summary
  - Create new admin with role preset (ADMIN/FINANCE/OPERATIONS/SUPPORT)
  - Permission editor: toggle all 47 permissions grouped by domain
  - maxRefundAmount per-user credit limit
  - Activate/deactivate admin accounts
  - SUPER_ADMIN only (403 for all other roles)
- app/api/admin/staff/route.ts: GET list + POST create admin
- app/api/admin/staff/[staffId]/permissions/route.ts: PATCH update permissions
- app/api/admin/staff/[staffId]/status/route.ts: PATCH activate/deactivate
- AdminNav: Admin Users link added under Users group
- Prisma regenerated (permissions field now in client)

tsc --noEmit: 0 errors