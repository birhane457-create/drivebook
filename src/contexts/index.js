/**
 * Contexts barrel — re-exports all React contexts from a single entry point.
 * Import from @/contexts instead of individual files for cleaner imports.
 */
export { AuthProvider, useAuth } from '@/lib/AuthContext';